# AGENT_CONSTITUTION.md — Personal Website Agent 宪法

> 项目：Personal Website（个人网站 · 接单交付凭证）
> 版本：v0.1 · 立项门 · 阶段 4
> 本文件是为本项目所有 AI 智能体（识图/编码/QA/写作）立的最短硬规矩。
> 目标：**方向不漂移、诚实不越红线、维护靠内容不靠改代码、成本可控**。
> 原则：短 / 硬 / 可直接判断对错。细则不进本文件。

---

## 一、项目本质（一切决定的前提）

1. 本项目是**纯静态单页站**：无后端、无数据库、无登录、无接口。任何智能体不得引入或建议引入后端/数据库/登录/重型前端框架。
2. 本项目的价值 = **内容驱动的接单交付凭证**。核心资产是「把一团乱麻，理成能用的系统」这一可信定位 + 真实可信的多面交付能力展示。
3. 视觉气质锁定为「**精品咨询 / 建筑事务所 / 工业设计工作室的克制工程感 + 个人档案/卷宗母题**」：骨白/暖灰白底、近黑文字、大留白、巨大衬线标题、细线+编号、克制动效。样式优先级高于花哨功能。具体配色/字体/排版见 `docs/视觉方向-v3-架构门.md`。

## 二、诚实底线（最高优先级，违反即停）

4. **禁止**：编造任何未做过的项目、未上线的事、不存在的成果、捏造的量级数字（如"20+模块"若无据）。所有展示内容必须有真实来源（`docs/` 或 `station-plan-v1.md` 或用户提供的材料）。
5. **禁止**：伪造真人头像、官方 logo、客户数据、未授权截图。
6. **禁止**：点名真实公司名 / 客户名 / 乙方名。一律匿名化（如「一家工业类企业」「工程用车助手」）。
7. **禁止**：把"正在学/会用但不精/开发中"写成"已做过/精通/已上线"。诚实标注三档（已交付 / 工具会用 / 边界诚实）。
8. **禁止**：虚构量级锚点。能用已知事实（8年/已上线/从零搭建）就用，不确定的不写。

## 三、项目专属条款（≥5）

9. **内容即真源**：成长记录、案例、能力条目一律放 `src/content/`（md + Frontmatter）。改内容只改这里，不得在组件里硬编码业务内容。
10. **匿名化是硬约束**：所有展示案例、截图、文字，先过「脱敏检查」（公司/客户/logo/姓名/敏感数据）再上站。
11. **9 分区结构不得随意增删**：首屏/品牌故事/四梯队能力/做过的事/项目案例/成长记录/做事的规矩/联系/底部。改分区需先改 `docs/a-立项文档.md` 并经用户确认。
12. **排位规则**：「做过的事」时间线必须真实时间顺序；「项目案例」用说服力排序（工程用车助手第 1 位）。不得为"好看"造假时间轴。
13. **MVP 思维**：任何改动必须先保证"能上线接单闭环"（定位→能力→证据→联系）。不得为追求完美无限推迟。

## 四、技术/架构硬规则

14. **框架优先，禁止自造**：用 Astro 官方构建/路由/内容集合；用原生 CSS + token；不引入 React/Vue、不手写 HTML 生成器、不自造 CSS 框架。
15. **必须维护**：`src/styles/` 只负责设计 token（配色/字体/间距），全局统一。
16. **read_only 目录**：`node_modules`、`dist`、`.git`、日志不外读不外搜（防上下文污染）。
17. **无 JS 也要能用**：关键内容在 JS 失败/禁用时仍完整可见；动效必须尊重 `prefers-reduced-motion`。

## 五、成本与上下文（每次会话）

18. **一事一议**：一个独立任务一个会话，不堆不相关内容；上下文过长先 `/compact` 或开新会话。
19. **会话熔断**：单会话消息数 ≤ 60，到顶主动建议开新会话，不硬撑。
20. **工具输出过滤**：只回关键证据（命令+关键行），不整屏塞 `ls`/`cat`/`grep`。
21. **模型分层**：测试/检查用轻量模型，核心内容生成用付费主力，复杂审查用高端模型。

## 六、禁止事项汇总（≥5）

22. 禁止写业务代码（未过架构/业务门不落代码）。
23. 禁止在组件里硬编码业务文案/内容。
24. 禁止把未脱敏信息、公司/客户名、真实 logo 放进站。
25. 禁止用"建议/尽量/可能"等软词替代硬规则。
26. 禁止让用户"从 2-3 个方案里选"——必须给唯一推荐并说明依据。
27. 禁止在立项门未过前进入参考图/实现。

## 七、验收标准

28. 任何产出必须**可检查**：有明确输入/输出、有证据（文件路径/命令输出/截图）。
29. 站上线前验收：9 分区完整、匿名化合规、桌面/移动无溢出、加微信可用、有可达 URL。
30. 一门一验：未过立项门不进架构门，未过架构门不进业务门，未过业务门不进上线门。

---

## 补充说明（非规则，供理解）

- 本宪法是"长期原则"；技术细节、配色 token 等放架构设计/真源文档。
- 各 IDE 入口文件（AGENTS.md / CLAUDE.md / .cursor/rules / .trae/rules）只写**引用本文件**，不复制内容。

## 待确认

- [ ] 本宪法 v0.1 是否认可（可增删条款）
- [ ] 各 IDE 入口文件路径（用哪些 IDE：Codex / Cursor / Claude / Trae）